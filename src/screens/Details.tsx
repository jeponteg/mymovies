import React, { FC } from 'react'
import { useNavigation } from '@react-navigation/native';
import { View, Image } from 'react-native'

interface Props {
    image:string;
}

const Details = ({image}: Props) => {
    return(
null
    )
}

export default Details